About the Project:

We have implemented code using the concept of convolutional neural network for the purpose of image classification.
We have used the following Pythong libraries:
numpy, tensorflow, scikit-learn. We used various methods that belong to these libraries.

